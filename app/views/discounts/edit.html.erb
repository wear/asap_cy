
<div class="info">
	<h2>编辑折扣</h2>
	<div></div>
</div>
<% form_for(@discount,:url => user_discount_path(current_user,@discount), :html => { :multipart => true }, :method => :put) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => "form", :locals => { :f => f }%>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', user_discount_path(current_user,@discount) %> |
<%= link_to 'Back', user_discounts_path(current_user) %>
